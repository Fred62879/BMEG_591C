0) please put `RawData.mat` in the same folder with code.

1) run reference processing via
`reference_processing();`

this prints dispersion coefficients finally.

2) perform volume processing via
`volume_processing();`

3) perform volume correction via
`volume_correction();`
which saves images and plots.
